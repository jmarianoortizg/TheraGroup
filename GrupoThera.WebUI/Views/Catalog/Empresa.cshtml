@model GrupoThera.Entities.Models.Catalog.CatalogModel
@{
    ViewBag.Title = "EmpresaView";
    var allItems = Model.AllEmpresa.Count;
}

<!-- Page Header -->
<div class="content bg-gray-lighter">
    <div class="row items-push">
        <div class="col-sm-7">
            <h1 class="page-heading">
                Catalogo Empresa <small>Creacion / Edicion / Eliminacion</small>
            </h1>
        </div>
    </div>
</div>
<!-- END Page Header -->
<!-- Page Content -->
<div class="content">
    <div class="block">
        <ul class="nav nav-tabs" data-toggle="tabs">
            <li class="active">
                <a href="#catalog-new">Nuevo</a>
            </li>
            <li>
                <a href="#catalog-list">Listado</a>
            </li>
            <li>
                <a href="#catalog-edit">Edicion</a>
            </li>
        </ul>
        <div class="block-content tab-content bg-white">
            <!-- Nuevo -->
            <div class="tab-pane fade fade-up in active" id="catalog-new">                
                <!-- New Item -->
                    @using (Html.BeginForm("CreateEmpresa", "Catalog", FormMethod.Post, new { id = "formCreateEmpresa", enctype = "multipart/form-data", @class = "form-horizontal row-border grid-form", onsubmit = "return validateCreateEmpresa()" }))
                    {
                        <fieldset>
                            <legend>Creacion Nuevo Item</legend>
                            <div data-row-span="5">
                                <div data-field-span="2">
                                    <label>Nombre</label>
                                    @Html.TextBoxFor(model => model.Empresa.nombre, new { id = "nombre", name = "nombre", @class = "form-control required", type = "text" })
                                </div>
                                <div data-field-span="2">
                                    <label>Razon Social</label>
                                    @Html.TextBoxFor(model => model.Empresa.razonSocial, new { id = "razonSocial", name = "razonSocial", @class = "form-control required", type = "text" })
                                </div> 
                                <div data-field-span="1">
                                    <label>RFC</label>
                                    @Html.TextBoxFor(model => model.Empresa.rfc, new { id = "rfc", name = "rfc", @class = "form-control required", type = "text" })
                                </div> 
                            </div>
                            <div data-row-span="1">
                                <div data-field-span="1">
                                    <label>Direccion</label>
                                    @Html.TextBoxFor(model => model.Empresa.direccion, new { id = "direccion", name = "direccion", @class = "form-control required", type = "text" })
                                </div> 
                            </div>
                            <div data-row-span="5">
                                <div data-field-span="1">
                                    <label>Telefono</label>
                                    @Html.TextBoxFor(model => model.Empresa.telefono, new { id = "telefono", name = "telefono", @class = "form-control required", type = "text" })
                                </div>
                                <div data-field-span="1">
                                    <label>Telefono2</label>
                                    @Html.TextBoxFor(model => model.Empresa.telefono2, new { id = "telefono2", name = "telefono2", @class = "form-control required", type = "text" })
                                </div>
                                <div data-field-span="1">
                                    <label>Email</label>
                                    @Html.TextBoxFor(model => model.Empresa.email, new { id = "email", name = "email", @class = "form-control required", type = "text" })
                                </div>
                                <div data-field-span="1">
                                    <label>Contacto</label>
                                    @Html.TextBoxFor(model => model.Empresa.contacto, new { id = "contacto", name = "contacto", @class = "form-control required", type = "text" })
                                </div>
                                <div data-field-span="1">
                                    <label>Formato</label>
                                    @Html.TextBoxFor(model => model.Empresa.formato, new { id = "formato", name = "formato", @class = "form-control required", type = "text" })
                                </div>
                            </div>
                        </fieldset>
                     
                        <br /><br />
                        <div class="clearfix pt-md">
                            <div class="pull-right">
                                <button class="btn-primary btn">Crear Nueva Empresa</button>
                            </div>
                        </div>
                    }
                </div>
            <!-- END Nuevo -->
            
            <!-- Listado -->
            <div class="tab-pane fade fade-up" id="catalog-list">
                <div class="border-b push-30">
                    <h2 class="push-10">@allItems <span class="h5 font-w400 text-muted">Items Encontrados</span></h2>
                </div>
                <table class="table table-striped table-vcenter">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Razon Social</th>
                            <th>RFC</th>
                            <th class="text-center" style="width: 5%;">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.AllEmpresa)
                        {
                            <tr>
                                <td class="">@Html.DisplayFor(modelItem => item.nombre)</td>
                                <td class="">@Html.DisplayFor(modelItem => item.razonSocial)</td>
                                <td class="">@Html.DisplayFor(modelItem => item.rfc)</td>
                                <td class="text-center">
                                    <div class="btn-group">
                                        <a id="@Html.DisplayFor(modelItem => item.empresaId)" class="btn btn-xs btn-default clsEditEmpresa" type="button"  title="Edicion"><i class="fa fa-pencil"></i></a>
                                        <a id="@Html.DisplayFor(modelItem => item.empresaId)" class="btn btn-xs btn-default clsDeleteEmpresa" type="button"  title="Eliminar"><i class="fa fa-times"></i></a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!-- END Listado -->

            <!-- Edicion -->
            <div class="tab-pane fade fade-up" id="catalog-edit">                
                <div class="col-md-12" id="divEditionEmpresa">
                    <div class="border-b push-30">
                        <h2 class="push-10"><span class="h5 font-w400 text-muted">Selecciona un item para la edicion</span></h2>
                    </div> 
                </div>
            </div>
            <!-- END Edicion -->
        </div>
    </div>
</div>

@section Scripts {

    <script src="@Url.Content("~/Content/plugins/jquery-validation/dist/jquery.js")"></script>
    <script src="@Url.Content("~/Content/plugins/jquery-validation/dist/jquery.validate.js")"></script>
    <script src="@Url.Content("~/Content/plugins/jquery-validation/dist/additional-methods.min.js")"></script>

    <script>

        $(document).ready(function () {

            $.notify({ icon: "fa fa-info-circle", message: 'Datos Cargados Correctamente' }, { type: 'info' });

            $(".clsEditEmpresa").on('click', function (e) {

                var model = {
                    "idEmpresaSelected": $(this).attr("id")
                };

                $.ajax({
                    url: '@Url.Action("EmpresaEdit", "Catalog")',
                    async: false,
                    type: 'POST',
                    cache: false,
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(model),
                    success: function (result) {
                        $.notify({ icon: "fa fa-info-circle", message: 'Datos Cargados para edicion' }, { type: 'info' });
                        $("#divEditionEmpresa").html(result);
                    }
                });
            });

            $(".clsDeleteEmpresa").on('click', function (e) {
                var model = {
                    "idEmpresaSelected": $(this).attr("id")
                };
                var item = $(this).closest("tr");
                $.ajax({
                    url: '@Url.Action("DeleteEmpresa", "Catalog")',
                    async: false,
                    type: 'POST',
                    cache: false,
                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(model),
                    success: function (result) {
                        if (result.success == true) {
                            item.remove();
                            $("#divEditionEmpresa").html("<div class='border-b push-30'><h2 class='push-10'><span class='h5 font-w400 text-muted'>Selecciona un item para la edicion</span></h2></div> ");
                            $.notify({ icon: "fa fa-check", message: 'Eliminacion Correcta' }, { type: 'success' });
                        } else {
                            $.notify({ icon: "fa fa-info-circle", message: 'Error Localizado: Observar la seccion de Edicion' }, { type: 'info' });
                            $("#divEditionEmpresa").html(result.responseText);
                        }
                    }
                });
            });
        });

        function validateEditEmpresa() {
            $("#formEditEmpresa").validate({
                wrapper: 'span',
                errorPlacement: function (error, element) {
                    error.css({ 'padding-left': '10px', 'margin-right': '20px', 'padding-bottom': '2px' });
                    error.addClass("arrow")
                    error.insertAfter(element);
                },
                rules: {

                },
                messages: {

                }
            });
            var band = $('#formEditEmpresa').validate().form();

            return band;
        }


        function validateCreateEmpresa() {
            $("#formCreateEmpresa").validate({
                wrapper: 'span',
                errorPlacement: function (error, element) {
                    error.css({ 'padding-left': '10px', 'margin-right': '20px', 'padding-bottom': '2px' });
                    error.addClass("arrow")
                    error.insertAfter(element);
                },
                rules: {

                },
                messages: {

                }
            });
            var band = $('#formCreateEmpresa').validate().form();

            return band;
        }
    </script>
}